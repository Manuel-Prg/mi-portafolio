<section class="hero">
  <div class="hero-content">
    <div class="profile-image-container">
      <img 
        src="/foto-perfil.jpg" 
        alt="Foto de perfil" 
        class="profile-image"
      />
    </div>
    <h1>Desarrollador Multiplataforma</h1>
    <p class="subtitle">Creando experiencias digitales excepcionales</p>
    <a href="#proyectos" class="cta-button">Ver mis proyectos</a>
    <div class="scroll-indicator">
      <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <polyline points="6 9 12 15 18 9"></polyline>
      </svg>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
    text-align: center;
  }

  .hero-content {
    max-width: 800px;
    animation: fadeInUp 1s ease-out;
  }

  .profile-image-container {
    margin-bottom: 2rem;
    animation: float 3s ease-in-out infinite;
  }

  .profile-image {
    width: 180px;
    height: 180px;
    border-radius: 50%;
    object-fit: cover;
    border: 4px solid rgba(102, 126, 234, 0.5);
    box-shadow: 0 10px 40px rgba(102, 126, 234, 0.4);
    transition: all 0.3s;
  }

  .profile-image:hover {
    transform: scale(1.05);
    box-shadow: 0 15px 50px rgba(102, 126, 234, 0.6);
  }

  h1 {
    font-size: clamp(2.5rem, 8vw, 5rem);
    font-weight: 800;
    margin-bottom: 1rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #f093fb 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1.2;
  }

  .subtitle {
    font-size: 1.5rem;
    color: #a0a0a0;
    margin-bottom: 2rem;
    transition: color 0.3s;
  }

  :global(body.light-mode) .subtitle {
    color: #666;
  }

  .cta-button {
    display: inline-block;
    padding: 1rem 2.5rem;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    text-decoration: none;
    border-radius: 50px;
    font-weight: 600;
    transition: transform 0.3s, box-shadow 0.3s;
    box-shadow: 0 10px 30px rgba(102, 126, 234, 0.4);
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 15px 40px rgba(102, 126, 234, 0.6);
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    animation: bounce 2s infinite;
    color: #ffffff;
    transition: color 0.3s;
  }

  :global(body.light-mode) .scroll-indicator {
    color: #1a1a1a;
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateX(-50%) translateY(0);
    }
    40% {
      transform: translateX(-50%) translateY(-10px);
    }
    60% {
      transform: translateX(-50%) translateY(-5px);
    }
  }

  @media (max-width: 768px) {
    h1 {
      font-size: 2.5rem;
    }

    .subtitle {
      font-size: 1.2rem;
    }

    .profile-image {
      width: 150px;
      height: 150px;
    }
  }
</style>
